using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using Microsoft.Extensions.Configuration;

namespace TimerEmail
{
    class teste1
    {
        //Lógica implatada para 

        //private static IConfigurationRoot _config;

        //static void Main(string[] args)
        //{
        //    CarregarJson();

        //    var hourToSend = DateTime.Now.ToString("t");

        //    if (hourToSend == _config["OthersConfig:hourSend"])
        //    {
        //        //cliente.Send(message);
        //        Console.WriteLine($"Email enviado com sucesso na data de: {DateTime.Now:D} as {DateTime.Now:T}");
        //    }
        //    else Console.WriteLine("Não foi possível o envio do Email.");
        //}

        //private static void CarregarJson()
        //{
        //    var builder = new ConfigurationBuilder()
        //        .SetBasePath(Directory.GetCurrentDirectory())
        //        .AddJsonFile($"appsettings.json");
        //    _config = builder.Build();
        //}

    }
}
